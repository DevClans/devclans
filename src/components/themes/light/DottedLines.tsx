import React from "react";

const DottedLines = () => {
  return (
    <div className="flex flex-col absolute inset-0">
      <div className="text-nbDottedLineColor text-lg font-regular">
        <span className="whitespace-nowrap">
          {Array.from({ length: 100 }, () => "-").join(" ")}
        </span>
      </div>
      <div className="text-nbDottedLineColor text-lg font-regular">
        <span className="whitespace-nowrap">
          {Array.from({ length: 100 }, () => "-").join(" ")}
        </span>
      </div>
      <div className="text-nbDottedLineColor text-lg font-regular">
        <span className="whitespace-nowrap">
          {Array.from({ length: 100 }, () => "-").join(" ")}
        </span>
      </div>
      <div className="text-nbDottedLineColor text-lg font-regular">
        <span className="whitespace-nowrap">
          {Array.from({ length: 100 }, () => "-").join(" ")}
        </span>
      </div>
      <div className="text-nbDottedLineColor text-lg font-regular">
        <span className="whitespace-nowrap">
          {Array.from({ length: 100 }, () => "-").join(" ")}
        </span>
      </div>
      <div className="text-nbDottedLineColor text-lg font-regular">
        <span className="whitespace-nowrap">
          {Array.from({ length: 100 }, () => "-").join(" ")}
        </span>
      </div>
      <div className="text-nbDottedLineColor text-lg font-regular">
        <span className="whitespace-nowrap">
          {Array.from({ length: 100 }, () => "-").join(" ")}
        </span>
      </div>
      <div className="text-nbDottedLineColor text-lg font-regular">
        <span className="whitespace-nowrap">
          {Array.from({ length: 100 }, () => "-").join(" ")}
        </span>
      </div>
      <div className="text-nbDottedLineColor text-lg font-regular">
        <span className="whitespace-nowrap">
          {Array.from({ length: 100 }, () => "-").join(" ")}
        </span>
      </div>
      <div className="text-nbDottedLineColor text-lg font-regular">
        <span className="whitespace-nowrap">
          {Array.from({ length: 100 }, () => "-").join(" ")}
        </span>
      </div>
    </div>
  );
};

export default DottedLines;
