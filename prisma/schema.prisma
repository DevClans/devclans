// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

// model User {
//   id        String   @id @default(auto()) @map("_id") @db.ObjectId
//   email     String   @unique
//   discordId String   @unique
//   username  String   @unique
//   avatar    String
//   Level     String
//   createdAt DateTime
//   updatedAt DateTime
//   skills    String[]
//   projects  String[]
// 
model User {
  id        String        @id @default(auto()) @map("_id") @db.ObjectId
  email     String        @unique
  discordId String        @unique
  username  String        @unique
  avatar    String        @default("https://www.google.com/url?sa=i&url=https%3A%2F%2Fwww.indiatvnews.com%2Fentertainment%2Fbollywood%2Fmunna-bhai-mbbs-turns-17-things-that-are-relevant-even-today-especially-jaadu-ki-jhappi-668827&psig=AOvVaw2oZam291pvgqxWcwlNuLaV&ust=1704637605808000&source=images&cd=vfe&opi=89978449&ved=0CBMQjRxqFwoTCLi2_I78yIMDFQAAAAAdAAAAABAE")
  Level     String        @default("Beginner")
  createdAt DateTime      @default(now())
  updatedAt DateTime      @default(now())
  skills    String[]
  projects  ProjectUser[]
  password  String
}

model Project {
  id        String        @id @default(auto()) @map("_id") @db.ObjectId
  name      String
  createdAt DateTime
  updatedAt DateTime
  users     ProjectUser[]
}

model ProjectUser {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  user      User     @relation(fields: [userId], references: [id])
  userId    String
  project   Project  @relation(fields: [projectId], references: [id])
  projectId String
  createdAt DateTime
  updatedAt DateTime
}
